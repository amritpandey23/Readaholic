# Book Reviews by Amrit

## Installation
- Clone
- virtual env: `python3 -m venv venv`
- activate venv: `. venv/bin/activate`
- load env vars: `. env.sh`
- install depen: `pip3 install -r req.txt`
- run: `flask run`
